{{- /*

SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if .Values.conversations_frontend.pdb.create }}
apiVersion: {{ include "common.capabilities.policy.apiVersion" . }}
kind: PodDisruptionBudget
metadata:
  name: {{ include "common.names.fullname" . }}-frontend
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: conversations-frontend
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.conversations_frontend.pdb.minAvailable }}
  minAvailable: {{ .Values.conversations_frontend.pdb.minAvailable }}
  {{- end  }}
  {{- if or .Values.conversations_frontend.pdb.maxUnavailable ( not .Values.conversations_frontend.pdb.minAvailable ) }}
  maxUnavailable: {{ .Values.conversations_frontend.pdb.maxUnavailable | default 1 }}
  {{- end  }}
  {{- $podLabels := include "common.tplvalues.merge" ( dict "values" ( list .Values.conversations_frontend.podLabels .Values.commonLabels ) "context" . ) }}
  selector:
    matchLabels: {{- include "common.labels.matchLabels" ( dict "customLabels" $podLabels "context" $ ) | nindent 6 }}
      app.kubernetes.io/component: conversations-frontend
{{- end }}
