{{- /*

SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if .Values.find.celery.pdb.create }}
apiVersion: {{ include "common.capabilities.policy.apiVersion" . }}
kind: PodDisruptionBudget
metadata:
  name: {{ include "common.names.fullname" . }}-celery
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: find-backend-celery
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.find.celery.pdb.minAvailable }}
  minAvailable: {{ .Values.find.celery.pdb.minAvailable }}
  {{- end  }}
  {{- if or .Values.find.celery.pdb.maxUnavailable ( not .Values.find.celery.pdb.minAvailable ) }}
  maxUnavailable: {{ .Values.find.celery.pdb.maxUnavailable | default 1 }}
  {{- end  }}
  {{- $podLabels := include "common.tplvalues.merge" ( dict "values" ( list .Values.find.celery.podLabels .Values.commonLabels ) "context" . ) }}
  selector:
    matchLabels: {{- include "common.labels.matchLabels" ( dict "customLabels" $podLabels "context" $ ) | nindent 6 }}
      app.kubernetes.io/component: find-backend-celery
{{- end }}
