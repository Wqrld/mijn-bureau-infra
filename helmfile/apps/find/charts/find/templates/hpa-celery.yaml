{{- /*

SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if .Values.find.celery.autoscaling.hpa.enabled }}
apiVersion: {{ include "common.capabilities.hpa.apiVersion" ( dict "context" $ ) }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "find.celery.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: find-backend-celery
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
    kind: Deployment
    name: {{ include "find.celery.fullname" . }}
  minReplicas: {{ .Values.find.celery.autoscaling.hpa.minReplicas }}
  maxReplicas: {{ .Values.find.celery.autoscaling.hpa.maxReplicas }}
  metrics:
    {{- if .Values.find.celery.autoscaling.hpa.targetMemory }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.find.celery.autoscaling.hpa.targetMemory }}
    {{- end }}
    {{- if .Values.find.celery.autoscaling.hpa.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.find.celery.autoscaling.hpa.targetCPU }}
    {{- end }}
{{- end }}
